import { makeStyles } from '@material-ui/core/styles';

export default makeStyles(() => ({
    aboutintro: {
        backgroundColor: 'white',
        overflowY: 'scroll',
        height: '100vh',

    }
}));

